{% extends 'admin.html.twig' %}

{% block content %}
    <h1>Modifier : <b>{{ class.title }}</b></h1>
    <hr>

    {{ include('classes/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-primary" href="{{ path('classes_index') }}">Retour</a>

{% endblock %}
